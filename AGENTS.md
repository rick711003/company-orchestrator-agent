# Company Orchestrator Agent Project Contract

Use `ORCHESTRATOR_AGENT.md` and `DELIVERY_HANDOFF_STANDARD.md` as the shared operating contract. Preserve unrelated changes, avoid destructive Git operations, never publish or contact people without explicit authorization, and validate with `npm run verify`.
